os: osx
osx_image: xcode10

before_script:
  - git clone https://github.com/tensorflow/tensorflow

addons:
  homebrew:
    taps:
      - bazelbuild/tap
      - homebrew/cask-versions
    casks: 
      - java8
      - bazel
    update: true
